// import moods from "./data/mood.json";

// const category = moods.map((el) => el.title);

// const mood_images = {};
// moods.forEach((mood) => {
//   const img = new Image();
//   img.src = require(`./wallpapers/${mood.cover}`).default;
//   //  const img_src = (
//   //   <img src={require(`./wallpapers/${mood.cover}`).default} alt={mood.title} />
//   // );
//   mood_images[mood.title] = img;
// });

// export const moodsCategory = category;
// export const moodImages = mood_images;
